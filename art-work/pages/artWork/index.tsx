import ArtWorkModule from "@/modules/artWork/module";
import { NextPage } from "next";
import { ReactElement } from "react";

const ArtWorkPage: NextPage = (): ReactElement => {
  return <ArtWorkModule />;
};

export default ArtWorkPage;
